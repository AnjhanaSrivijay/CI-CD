name: Lighthouse Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]


jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
     - name: Audit URLs using Lighthouse
       uses: foo-software/lighthouse-check-action@master
       with:
         urls: 'https://webshop-agil-testautomatiserare.netlify.app'
